# Database (R2.2 — the DB lives OUTSIDE the repo tree).
# The live SQLite file and its backups sit in ~/anton-data/ (backups under
# ~/anton-data/backups/), never next to the source. Use an ABSOLUTE path — note
# the FOUR slashes after `sqlite:`. Replace the user segment for your machine.
# Backup convention: cp the live DB to
#   ~/anton-data/backups/shoe_deals.db.<YYYY-MM-DD>-<label>.bak
# before any structural/data-moving migration (CLAUDE.md §9 / design_decisions E4).
DATABASE_URL=sqlite:////Users/you/anton-data/shoe_deals.db

# API Settings
# API_HOST is the bind interface. Default (loopback-only) is the safe post-R2.1
# choice — the server is reachable only from this machine. To serve the LAN,
# set API_HOST=0.0.0.0; now safe because the bearer token is required (R2.1).
API_HOST=127.0.0.1
API_PORT=8000
DEBUG=True

# Auth (RA1.1 — per-client named tokens). REQUIRED: the server refuses to boot
# without at least one entry. Named format: "client:token,client:token,...".
# Each client gets its own revocable token — revoking one doesn't affect others.
# Clients:
#   desktop  — Claude Desktop (mcp-remote --header "Authorization: Bearer <token>")
#   loopback — Son of Anton's self-connection to /mcp (chat_service)
# Generate tokens with: python -c "import secrets; print(secrets.token_hex(32))"
#
# RA2.1: the `spa` token was RETIRED. The SPA now authenticates with an httpOnly
# session cookie issued after a password login (see ANTON_LOGIN_PASSWORD below),
# so the built bundle carries no bearer secret. Remove any `spa:...` entry here
# (revoked) and drop VITE_ANTON_SECRET from frontend/.env.
ANTON_TOKENS=desktop:REPLACE_ME_desktop,loopback:REPLACE_ME_loopback

# OAuth 2.1 connector auth (RA1.1b — replaces capability-URL, design_decisions E9).
# Set these to enable the claude.ai connector via OAuth 2.1 + PKCE.
# All four vars must be set together; leave blank to disable OAuth.
#
#   ANTON_HOST_URL       — the public HTTPS base URL of this server (issuer URL).
#                          Example: https://anton.example.com
#   ANTON_LOGIN_PASSWORD — a strong password the user types (a) in the browser at
#                          /oauth/login during the connector authorization flow,
#                          and (b) at the SPA login (POST /api/auth/session, RA2.1)
#                          to receive the session cookie. One owner, one password.
#   ANTON_OAUTH_CLIENT_ID     — arbitrary client identifier (e.g. "claude-ai-connector")
#   ANTON_OAUTH_CLIENT_SECRET — optional; leave blank for public clients
#   ANTON_OAUTH_REDIRECT_URI  — the redirect URI registered in the claude.ai connector
#
# Generate the login password: python3 -c "import secrets; print(secrets.token_hex(24))"
ANTON_HOST_URL=https://anton.musasouled.com
ANTON_LOGIN_PASSWORD=
ANTON_OAUTH_CLIENT_ID=
ANTON_OAUTH_CLIENT_SECRET=
ANTON_OAUTH_REDIRECT_URI=

# SPA session cookie (RA2.1). ANTON_LOGIN_PASSWORD (above) also gates the SPA
# login. SESSION_TTL_DAYS is the "stay signed in" lifetime of the anton_session
# cookie/row (default 14). The CSRF Origin check on cookie writes uses
# ANTON_HOST_URL; when it is unset (local dev) the check is skipped and
# SameSite=Lax is the guard.
SESSION_TTL_DAYS=14

# Chat rate limiting (R2 — the R2.1-adjacent throttle). Backstops paid LLM spend:
# R2.1 stopped *anonymous* spend, this caps an authenticated-but-looping client
# on POST /api/chat/message. Token bucket, per client IP, in-process (429 on
# exceed). Defaults below are generous for a human, a hard stop for a runaway
# loop; raise them if legitimate use ever hits the limit. See design_decisions E8.
CHAT_RATE_LIMIT_PER_MINUTE=20
CHAT_RATE_LIMIT_BURST=20

# Scraping Settings
SCRAPE_DELAY_SECONDS=3
MAX_CONCURRENT_SCRAPERS=2
USER_AGENT=Personal Shoe Deal Finder Bot (contact@example.com)

# CORS (for development)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,https://anton.musasouled.com

# Scheduling
SCRAPE_SCHEDULE_HOURS=6  # Run scraping every 6 hours

# AI providers (for the chat assistant)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GOOGLE_API_KEY=

# MCP server URL used by the chat assistant (defaults to the local server)
MCP_SERVER_URL=http://localhost:8000/mcp

# COROS GPS watch sync (optional — leave blank to disable)
# Get these from the COROS Open Platform developer portal
COROS_CLIENT_ID=
COROS_CLIENT_SECRET=
COROS_ACCESS_TOKEN=
COROS_OPEN_ID=
